name: client
description: Flutter app that use gRPC || proof of concept
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.10

dependencies:
  bloc: ^8.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  isar: ^2.5.0
  isar_flutter_libs: ^2.5.0

dev_dependencies:
  bloc_test: ^9.1.0
  build_runner: ^2.0.0
  flutter_test:
    sdk: flutter
  isar_generator: ^2.5.0
  mocktail: ^0.3.0
  stores_api:
    path: packages/stores_api
  stores_repository:
    path: packages/stores_repository
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
